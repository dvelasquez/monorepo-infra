{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["package.json", "tsconfig.json", "astro.config.mjs", "src/**/*", "public/**/*"],
      "outputs": ["dist/**", ".astro/**"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}